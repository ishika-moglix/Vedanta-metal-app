// const initialState = {setIsLoggedIn: () => {},
// setIsSignedUp: () => {},
// };

// export default auth = (state = initialState, action) => {
//   const {type, payload} = action;
//   switch (type) {
//     case 'SET_LOGIN':
//       return {setIsLoggedIn: payload?.setIsLoggedIn || {}};
//     default:
//       return state;
//   }
// };
